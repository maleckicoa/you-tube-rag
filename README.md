Run the data_loader.py, this will:


tokens?
models used?

To Fetch youtube captions:
poetry run python -m src.data_loader create

To embedd the captions in a vector database:
poetry run python -m src.data_loader embed